# Node: input

**Category**: input
**Description**: Pause workflow and wait for user input (supports multiple questions)

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `questions` | `array` | Yes | `-` | List of questions to ask the user. |

## Output

Output schema for Input node - dictionary mapping question names to values.
